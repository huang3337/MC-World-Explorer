# PROJECT_ROADMAP.md

# MC World Explorer 项目路线图

## 项目定位

MC World Explorer 是一个独立于 Minecraft 客户端运行的存档浏览工具。

核心目标：

在不启动 Minecraft 的情况下快速浏览、管理和预览世界存档。

最终目标：

- 快速查看存档信息
- 浏览世界地图
- 定位建筑与基地
- 三维世界浏览
- 自动巡航展示
- 动态壁纸模式

------

# 技术规划

## 开发语言

Java 21

## UI框架

JavaFX

## 构建工具

Gradle

## 数据来源

Minecraft 存档文件

主要包括：

- level.dat
- region/*.mca
- playerdata/*.dat
- icon.png

------

# V0.1 存档管理器

## 目标

完成最小可用产品（MVP）。

## 功能

### 存档扫描

扫描 Minecraft 存档目录。

### 存档列表

显示：

- 世界名称
- Minecraft版本
- 游戏模式
- 最后游玩时间
- 世界图标

### 存档详情

显示：

- 世界名称
- 创建时间
- 最后游玩时间
- 游戏时间
- 种子
- 出生点坐标
- 玩家坐标

## 完成标准

能够正常扫描并显示存档信息。

------

# V0.1.1 代码质量加固

## 目标

基于 V0.1 代码审查发现的问题，进行代码结构优化、基础设施补充和存档解析兼容性加固。

保持正常存档既有展示逻辑；允许修复已经确认的识别和兼容性缺陷。

## 重点

- 引入日志框架（SLF4J + Logback）
- 统一错误处理策略
- 提取 GameType 枚举
- 清理死代码
- 代码规范整理
- 补齐 Gradle Wrapper 和 Windows 启动脚本
- 修复大型整合包 `level.dat` 读取额度不足的问题

## 详细任务清单

见 `docs/progress/V0.1.1.md`。

## 代码审查记录

活动问题见 `docs/issues/`，已解决问题见 `docs/issues/resolved/V0.1.1/`。

------

# V0.2 世界缩略图

## 功能

### Region解析

读取：

region/*.mca

### 世界俯视图生成

生成：

world_preview.png

### 缓存机制

首次生成后缓存。

### 程序内预览与导出

显示当前世界缩略图，并允许导出 PNG。

## 完成标准

能够生成、显示并导出世界俯视图。

------

# V0.2.1 多维度预览与洞穴分层

## 目标

在 V0.2 主世界地表缩略图基础上建立统一维度图层系统，让不同维度能够按需生成地表总览或洞穴高度带，同时保持存档只读和严格便携缓存。

## 功能

### 维度发现

- 支持主世界、下界和末地的标准 Region 目录。
- 支持经真实整合包样本验证的 `dimensions/<命名空间>/<维度路径>/region` Mod 维度入口。
- Mod 维度垂直范围从可解析区块的实际 Section 范围取得；无法可靠确定时明确失败，不凭路径或名称猜测。

### 统一图层

- `SURFACE_OVERVIEW`：在维度完整垂直范围内寻找最高可见地表。
- `HEIGHT_BAND`：按 32 格划分高度带，在范围内寻找上方至少保留两格可通行空间的最高地面。
- 地表总览和高度带使用同一图层接口，不为每个维度复制独立渲染流程。

### 默认图层

- 玩家当前位于目标维度且坐标可靠时，默认使用玩家 Y 所在高度带。
- 主世界、末地和未知 Mod 维度缺少可用坐标时默认使用地表总览。
- 下界缺少可用坐标时默认使用包含 `Y=64` 的高度带，避免首先显示基岩顶部。

### 程序内选择与缓存

- 在世界预览中选择维度、地表总览或具体高度带。
- 首次只生成默认图层，未缓存图层在用户选择时按需生成。
- 缓存按世界、维度、中心、图层类型、高度范围和渲染版本隔离。
- 所有缓存继续保存在程序根目录 `cache/`，不得写入 Minecraft 存档或 AppData。

## 完成标准

- 主世界、下界、末地和经真实样本确认支持的 Mod 维度共用同一图层接口。
- 主世界 V0.2 地表总览效果不因分层改造而退化。
- 下界内部高度带不会被顶部基岩遮蔽，洞穴和地下结构能够通过高度带查看。
- 已缓存图层能够直接复用，未缓存图层只在选择后生成。
- 自动化和真实存档验收证明相关存档文件的大小、修改时间和 SHA-256 均未改变。

## 不属于 V0.2.1

- 地图连续拖动、缩放、坐标跳转和标记系统。
- Mod 资源包或纹理加载。
- 全维度、全高度带预生成。
- 三维体素浏览或任何 Minecraft 存档写入功能。

完整决策见 `docs/decisions/DECISION-003.md`，设计基础见 `docs/superpowers/specs/2026-07-18-unified-dimension-layer-system-design.md`，开发状态见 `docs/progress/V0.2.1.md`。

------

# V0.3 地图浏览器

## 功能

### 地图缩放

支持放大缩小。

### 地图拖动

支持自由浏览。

### 坐标定位

输入坐标跳转。

### 标记系统

自动识别：

- 单人及多人玩家最后位置
- 出生点
- 下界传送门

### 玩家导航

- 从玩家列表选择名称。
- 自动切换到玩家所在的可用维度。
- 以地表总览、默认缩放和玩家坐标为中心显示。

## 完成标准

实现可平滑拖动缩放、坐标和玩家定位、标记开关、缓存清理与当前视口导出的地图浏览功能。

------

# V0.3.1 发布前完善

## 目标

完成 V0.3 交互地图的软件包发布前加固，并建立可核对的失败恢复、缓存维护、升级和候选包验收流程。

## 功能

- 地图块首次失败自动重试一次，持续失败后允许点击对应地图块单独重试。
- 玩家 Tooltip 显示名称、维度和坐标，玩家列表定位后短时高亮目标。
- 清理当前存档缓存前显示文件数量和容量，清理后保留当前视图并重新加载。
- 在不增加磁盘瓦片级别的前提下增加 `2×`、`4×` 逐方块视觉放大。
- 完成 V0.2.1 历史缓存与配置升级验证、Windows x64 候选包构建和启动检查。

## 完成标准

- 自动化、真实存档只读、人工功能、逐文件审查和候选包启动验证通过。
- 本地候选包从当前源码执行干净构建，并记录文件大小和 SHA-256。
- 提交、标签和 GitHub Release 仍由开发者单独确认。

详细设计见 `docs/superpowers/specs/2026-07-30-v0.3.1-release-polish-design.md`，开发状态见 `docs/progress/V0.3.1.md`。

------

# V0.4 三维技术可行性验证

## 目标

在不影响 V0.3.1 二维地图稳定基线的前提下，验证当前 Java 项目能否建立完整体素、基础网格、三维相机和可打包渲染后端。

本版本属于内部技术验证阶段，不承诺面向普通用户发布三维功能。

## 功能

### 完整方块状态试验

- 在现有 Section Palette 解码基础上，试验性保留方块名称和完整属性。
- 从一个或少量真实区块构造包含完整 Y 轴数据的体素表示。
- 验证负高度、跨 Section 和跨区块坐标转换。

### 简化三维网格

- 使用纯色或现有地图颜色显示简化方块。
- 生成基础立方体网格。
- 验证相邻方块面剔除。
- 实现最基本的旋转、俯仰和缩放相机。

### 渲染后端验证

- 隔离比较候选三维渲染后端。
- 验证 Windows x64 开发运行和便携打包可行性。
- 在实际设备上记录加载时间、内存、帧率和资源释放结果。
- 验证基础档和增强档共用同一数据与渲染管线的可行性。

## 完成标准

- 至少一个候选后端能够稳定显示真实存档的小范围立体区块。
- 区块坐标、负高度和跨 Section 数据经自动化及人工检查确认正确。
- 相机交互稳定，没有明显持续内存增长。
- 存在可行的 Windows x64 便携打包方案。
- 形成候选后端、风险和实测结果的对比结论。
- 渲染后端最终选择由开发者明确确认，并按重大技术决策规则记录。

## 不属于 V0.4

- 不接入正式主界面。
- 不加载原版纹理和复杂方块模型。
- 不实现自由飞行或动态区块加载。
- 不承诺最终帧率、视距和硬件要求。
- 不将未经整理的验证代码直接扩展为正式架构。

如果候选方案均无法满足基础要求，必须暂停三维路线并重新评估，不得直接进入 V0.5。

------

# V0.5 三维数据与渲染基础

## 目标

根据 V0.4 已确认的技术结论，建立能够长期维护的完整方块状态、立体区块数据和简化网格基础。

## 功能

### 完整三维数据模型

- 保存方块 ID、属性和可识别状态，不在解析阶段丢失三维所需信息。
- 建立 Section、Chunk 和世界坐标下的立体数据表示。
- 区分空气、实体方块、透明方块、流体和暂不支持方块。
- 保持 Region 和 NBT 数据严格只读。

### 网格生成基础

- 从立体区块数据生成简化立方体网格。
- 完成区块内部、跨 Section 和必要的相邻区块面剔除。
- 明确数据读取、网格生成和渲染后端之间的职责边界。
- 支持取消、错误隔离和明确的失败结果。

### 兼容与验证

- 使用主世界、下界、末地和已支持 Mod 维度进行真实区块验证。
- 损坏或不支持区块只影响局部结果。
- 保持 V0.3.1 二维采样、地图浏览和缓存行为不变。

## 完成标准

- 真实区块能够确定性地生成简化三维几何。
- 方块状态和坐标信息能够完整传递到网格生成阶段。
- 单个损坏或不支持区块不会导致整个预览任务失败。
- 自动化和真实存档验证证明 Minecraft 文件没有发生变化。
- 二维地图完整回归测试通过。
- 正式数据模型没有与单一渲染后端形成不必要耦合。

## 不属于 V0.5

- 不提供正式用户三维入口。
- 不加载完整原版资源。
- 不实现无限世界浏览。
- 不建立公开插件系统或提前设计完整 Mod 渲染框架。
- 不为了未知旧版本兼容堆积大量分支。

------

# V0.6 小范围三维预览

## 目标

提供第一个面向用户的三维功能：从二维地图选择受控区域，生成可以旋转观察的简化三维预览。

## 功能

### 二维范围选择

- 从二维地图进入三维范围选择状态。
- 使用跟随鼠标的半透明方形范围提示可转换区域。
- 方形范围根据二维地图缩放保持受控的实际方块范围。
- 用户确认区域后开始生成三维预览。
- 已完成转换的区域能够从二维地图进入对应三维预览。

具体选择框尺寸、状态表现和交互细节在 V0.6 开始前单独设计，不由路线图提前锁定。

### 简化三维预览

- 使用简化彩色方块显示选定区域的地形和建筑。
- 支持环绕旋转、俯仰和缩放。
- 保留世界、维度、中心坐标和选择范围信息。
- 支持返回二维地图来源位置。

### 任务与降级

- 后台生成并显示进度。
- 支持取消、旧结果隔离和明确失败提示。
- 基础档与增强档允许采用不同安全范围或质量设置，但共用同一实现。
- 无法识别的方块使用稳定颜色或占位模型，不阻止其他内容显示。

## 完成标准

- 用户能够完成“二维选择区域、生成、进入三维、返回二维”的完整流程。
- 真实存档中的小范围建筑和地形具有可辨识的立体结构。
- 核显基础档能够稳定完成受控范围预览。
- 切换世界、维度或取消任务时不会显示过时结果。
- 三维功能失败不会影响二维地图继续使用。
- 自动化、真实存档只读、人工交互和 Windows x64 包启动验收通过。

## 不属于 V0.6

- 不允许飞出已生成区域。
- 不实现无限视距或动态区块流送。
- 不要求原版纹理和复杂方块形状。
- 不加入生物、粒子、红石模拟、天气或时间系统。
- 不向 Minecraft 存档写入任何内容。

------

# V0.7 原版视觉资源

## 目标

在 V0.6 简化彩色方块基础上加载原版视觉资源，使常见地形和建筑更接近 Minecraft 原版外观，同时保留可靠降级路径。

## 功能

### 原版资源解析

- 从用户本地 Minecraft 资源中读取所需内容，不在项目中直接附带游戏资源。
- 建立 blockstate、方块模型、纹理和模型引用解析流程。
- 建立纹理图集和资源缓存。

### 分阶段模型支持

- 优先支持完整立方体和常见简单方块模型。
- 分阶段处理方块朝向、组合状态、透明、半透明、裁剪纹理和流体。
- 无法解析的原版或 Mod 方块继续使用稳定颜色或占位模型。
- 简化彩色模式作为兼容和诊断模式长期保留。

## 完成标准

- 常见原版地形与建筑方块能够正确显示纹理。
- 主要方块朝向和状态能够反映到模型。
- 资源缺失或版本不匹配不会导致整个场景失败。
- 纹理与模型缓存能够正确释放，不持续无界增长。
- 基础档与增强档能够使用同一资源管线。
- 真实存档中的建筑外观比 V0.6 更容易辨识。

## 不属于 V0.7

- 不要求一次覆盖所有特殊方块模型。
- 不承诺完整 Mod 资源和自定义模型兼容。
- 不实现实体、粒子、天气、动态光影或完整方块动画。
- 不提前实现自由世界流送。

如果完整模型系统风险过高，可以先限定为原版完整方块纹理和常见模型，其余内容继续安全降级。

------

# V0.8 交互式三维浏览

## 目标

将固定范围三维预览扩展为观察者式自由浏览，并建立按相机位置加载和卸载区块的运行时系统。

## 功能

### 自由移动

- 支持观察者式自由移动和鼠标观察。
- 不模拟生存模式、碰撞或游戏逻辑。
- 支持返回二维地图及来源坐标。

### 动态区块管理

- 根据相机位置按需加载周边区块。
- 离开范围的区块及时卸载。
- 使用有上限的后台并发、加载优先级、取消和旧结果隔离。
- 单个损坏区块只影响局部区域，并提供明确失败状态。
- 三维缓存与源 Region 变化建立明确失效关系。

### 性能档位

- 提供面向核显的基础档和面向独显的增强档。
- 两档共享数据、资源和渲染管线。
- 视距和质量上限根据 V0.4 至 V0.7 的实测结果确定。
- 加入必要的视锥剔除、网格复用和渲染批次优化。

## 完成标准

- 用户能够在真实世界中持续移动并加载新区域。
- 长时间浏览时内存和显存保持在可控范围，不持续无界增长。
- 区块加载不会长期阻塞界面或相机操作。
- 损坏区块只影响局部区域。
- 核显基础档能够使用受限视距稳定浏览。
- 真实存档读取前后保持完全不变。

## 不属于 V0.8

- 不实现碰撞、重力、生物 AI、红石逻辑、随机刻或流体模拟。
- 不实现编辑、放置、破坏方块或任何存档写入。
- 不实现自动巡航、展示模式和动态壁纸。
- 不以高端显卡表现作为基础档完成标准。

如果动态区块流送无法达到可接受稳定性，应保留 V0.6 的受控范围预览并插入加固阶段，不得强行进入 V0.9。

------

# V0.9 双模式整合与发布完善

## 目标

将二维地图和三维浏览整合为统一产品，并完成 V1.0 前的性能、兼容、升级和发布验收。

## 功能

### 二维与三维整合

- 明确二维地图模式和三维浏览模式入口。
- 支持从二维选择区域或坐标进入三维。
- 支持从三维返回二维来源位置。
- 共享世界、维度、坐标、玩家、出生点和其他已有标记。
- 三维功能故障时保持二维地图可用。

### 配置与缓存

- 完善三维视距、基础档和增强档设置。
- 完善资源缺失、区块失败和设备能力提示。
- 完成三维缓存统计、清理、升级和版本失效策略。
- 验证历史二维缓存和配置升级。

### 整体验收

- 执行性能分析、完整自动化、真实存档只读验证和逐文件审查。
- 完成长时间人工浏览验收。
- 构建 Windows x64 候选软件包并执行纯净启动及历史配置升级验证。
- 根据 V0.4 至 V0.8 的实测结果确认 V1.0 正式性能和兼容范围。

## 完成标准

- 二维与三维模式能够稳定往返。
- 旧版本配置和二维缓存不会导致升级失败。
- 所有活动高等级问题已解决或由开发者明确确认处理方式。
- 自动化、真实存档、长时间人工浏览和候选包验收通过。
- V1.0 最终范围、兼容版本和硬件要求得到开发者明确确认。

## 不属于 V0.9

- 不在发布完善阶段增加大型新功能。
- 不提前加入展示模式、自动巡航或动态壁纸。
- 不为了追求完整 Mod 兼容而推迟整个 V1.0。

------

# V1.0 二维与三维世界浏览器

## 目标

形成稳定、可发布的双模式 Minecraft 世界浏览器。

## 功能

### 二维地图模式

- 保留 V0.3.1 已完成的多维度地图、分层、拖动缩放、坐标定位、标记、缓存和导出能力。
- 用于快速总览、定位、导航和低占用浏览。

### 三维浏览模式

- 支持从二维地图选择区域或坐标进入三维。
- 支持小范围三维预览和观察者式自由浏览。
- 支持原版常见纹理与方块模型，并为不支持内容提供降级显示。
- 支持按需加载和卸载区块。

### 双模式共享

- 共享存档、维度、坐标和标记信息。
- 支持在二维和三维来源位置之间往返。
- Minecraft 存档始终只读。
- Windows x64 作为首个正式验证和发布平台。
- 代码和模块边界保留未来跨平台验证的可能性。

## 完成标准

- 用户无需启动 Minecraft，即可从存档列表进入二维地图或三维浏览。
- 二维和三维模式均达到 V0.9 最终确认的稳定性和性能门槛。
- 核显基础档和独显增强档具有明确且经过验证的设置范围。
- 真实存档只读、升级、缓存、错误恢复、人工功能和 Windows x64 软件包验收通过。
- 版本经过开发者最终确认后发布。

## V1.0 不包括

- Minecraft 客户端替代功能。
- 世界编辑、方块放置或存档写入。
- 生物 AI、红石、随机刻、流体等游戏模拟。
- 完整 Mod 客户端兼容。
- 光影、自动巡航或动态壁纸。

------

# V1.1 存档管理增强

## 目标

在二维和三维世界浏览主线稳定后，完善存档整理和检索能力。

## 功能

### 标签系统

例如：

- 生存
- 建筑
- 红石
- 工业

### 收藏夹

### 搜索系统

### 存档封面

## 完成标准

具备成熟存档管理能力，并能够与二维和三维入口协同工作。

------

# V0.4 至 V1.0 统一开发约束

## 阶段复核

每个版本结束时必须根据实际证据给出以下结论之一：

- 继续：主要风险已经验证，可以进入下一版本。
- 调整：方向可行，但需要缩小范围、修改方案或插入 V0.x.1 加固版本。
- 暂停：当前技术路线不满足要求，需要重新评估。
- 回退：试验结果损害二维模式或只读安全，应撤销试验性集成并保留稳定版本。

技术原型成功、单项功能完成或自动化测试通过，不等于版本完成，也不自动授权进入下一版本。

## 必须重新评估的情况

- 候选三维后端无法稳定运行或打包。
- 核显基础档无法显示最小受控区域。
- 内存或显存随浏览持续无界增长。
- 完整方块状态无法可靠解析。
- 原版资源加载方案存在不可接受的分发或兼容问题。
- 三维缓存无法随源 Region 变化可靠失效。
- 三维任务明显破坏二维地图响应或稳定性。
- 少数旧版或 Mod 格式要求引入过多复杂度。
- 三维实现要求写入 Minecraft 存档。
- 必须更换技术栈、UI 框架或大幅修改项目结构。

重大技术决策必须暂停开发，说明影响和备选方案，等待开发者明确确认后再记录和实施。

## 性能与平台

- 当前正式开发、测试、便携打包和发布目标为 Windows x64。
- 代码和模块边界避免无必要地绑定 Windows 专属能力。
- 基础档面向核显和普通笔记本，增强档面向独立显卡。
- 两档共用同一数据和渲染管线。
- 当前不提前写死帧率、视距和内存数字。
- V0.4 至 V0.8 逐阶段记录实测结果，V0.9 根据证据确认 V1.0 正式要求。

## 二维地图稳定基线

- V0.3.1 作为二维地图模式稳定基线长期保留。
- 严重缺陷、兼容性问题及与三维共享的坐标、维度、标记和缓存能力可以继续维护。
- 独立二维大型功能不得无理由打断当前三维阶段。
- 三维试验不得破坏已经验证的二维数据和渲染流程。
- 三维功能失败时，用户仍应能够正常使用二维地图。

## 版本完成与发布

- 开发期间遵守 `DEVELOPMENT_RULES.md` Rule 18，不创建未完成版本的阶段性提交。
- 完成自动化、真实存档只读验证、必要性能测量、Rule 16 审查和开发者人工验收后，才能确认版本完成。
- 是否打包、创建标签和发布 GitHub Release，由开发者单独确认。

------

# V2.0 展示模式

## 功能

### 自动巡航

### 建筑展示模式

### 时间控制

### 天气控制

## 完成标准

用于建筑展示与录像预览。

------

# V3.0 动态壁纸

## 功能

### 动态壁纸模式

### 自动镜头系统

### 长时间低功耗运行

## 完成标准

可作为桌面动态壁纸运行。

------

# 长期研究方向

以下内容暂不纳入正式开发计划：

- 光影支持
- 完整 Mod 资源包与自定义模型兼容
- 存档时间轴
- 离线区域模拟
- 世界分享系统

------

# 当前开发阶段

当前阶段：

V0.1.1 源码和发布前修正已进入远端 `main`。2026-07-13 已生成 Windows x64 便携版 ZIP，并通过控制台诊断、主窗口可视化和真实目录只读验收；标签 `v0.1.1`、正式 GitHub Release、ZIP 和 SHA-256 校验文件均已发布。

V0.2 已于 2026-07-14 正式开始，并于 2026-07-18 完成功能开发、自动化测试、真实存档只读验收和逐文件代码审查。中心点优先采用 `level.dat` 中主世界个人重生点，其次采用世界出生点，最后回退 `(0,0)`。开发者随后确认 V0.2 不单独构建软件包，其成果直接作为 V0.2.1 的开发基础。

V0.2.1 已于 2026-07-24 完成统一多维度图层系统、32 格洞穴高度带、分层缓存、按维度独立恢复的平滑高度滑块、自动化测试、真实存档只读验收、最终代码审查和人工界面验收。Windows x64 便携包已构建并通过实际启动验证，本版本使用统一提交、标签 `v0.2.1` 和 GitHub Release 发布。

V0.3 已于 2026-07-29 完成地图拖动、连续视觉缩放与五级缓存、X/Z 坐标定位、单人及多人玩家/出生点/下界传送门标记、玩家列表与跨维度定位、当前世界缓存清理和当前视口 PNG 导出，并通过全量自动化、真实四类维度图块及真实多人存档只读验收、JavaFX 启动检查、逐文件审查和开发者人工界面验收。开发者已明确允许统一提交并同步 GitHub；本版本不创建软件包和 Release，最后完善与发布评估延后到 V0.3.1。

V0.3.1 已于 2026-07-30 完成失败地图块有限重试与点击恢复、玩家身份 Tooltip 和定位高亮、缓存容量与实际清理反馈、`2×`/`4×` 高倍率视觉缩放、172 项自动化测试、真实存档只读验证、逐文件审查和开发者人工功能确认。Windows x64 软件包从统一版本提交执行干净构建，通过纯净解压启动和携带历史缓存/配置的升级启动验证，并使用标签 `v0.3.1` 和 GitHub Release 发布。

当前已完成或阶段性实现目标：

- 扫描原版及版本隔离存档
- 解析原版和大型整合包 `level.dat`
- 显示路线图约定的基础信息；创建时间明确采用存档文件夹的文件系统创建时间
- 完成日志、错误处理、类型安全和 UI 健壮性加固
- 支持选择游戏根目录、saves、versions、单个实例或世界目录
- 存档扫描在后台线程执行，界面显示加载和结果状态
- Windows x64 便携版内置 Java 21 运行时，用户无需安装 Java
- V0.2 已统一开发环境与便携版程序根目录，并完成日志本地化和不可写目录提示
- V0.2 已完成个人重生点、世界出生点和原点回退的中心点解析
- V0.2 已完成标准 `.mca` 文件头、区块定位以及 GZIP、ZLIB、未压缩区块解压
- V0.2 已完成两类现代 Palette/Section 布局和两种 long 数组位排列的地表采样
- V0.2 已完成固定范围 PNG、方块颜色、高度明暗和 `cache/` 缓存复用
- V0.2 已完成后台生成、进度显示、任务取消、缓存优先加载和程序内世界预览
- V0.2 已完成默认 `exports/` 一键导出、唯一文件名、已有文件保护和备用位置选择
- V0.2 已将自定义 Minecraft 目录保存到程序根目录 `config/settings.properties`，停止写 Windows Preferences
- V0.2 已完成 32 个生产文件的逐文件审查；记录的 2 个中等级问题均已修复
- V0.2.1 已支持主世界、下界、末地和 `dimensions/<namespace>/<path>/region` Mod 维度发现
- V0.2.1 已完成地表总览与 32 格高度带的统一请求、采样、生成和缓存流程
- V0.2.1 已完成玩家当前维度/高度默认规则、下界 `Y=64` 默认高度带和其他维度安全回退
- V0.2.1 已实现 JavaFX 维度选择、地表总览切换、按维度独立恢复的平滑高度滑块、后台按需生成、取消和缓存复用
- 2026-07-18 的 V0.2.1 阶段验收通过 27 个套件、105 个测试；启用当时的真实目录后 105 个测试全部通过
- 同次真实目录验收发现 25 个主世界、8 个下界、7 个末地和 54 个 Mod 维度入口，三类真实维度预览均生成有效图像且失败区块为 0
- 2026-07-18 的 V0.2.1 阶段验收已验证相关存档文件生成前后的大小、修改时间和 SHA-256 完全不变
- 2026-07-24 的 V0.2.1 最终验收通过 27 个套件、115 个测试；启用真实目录后 115 个测试全部通过
- V0.2.1 已完成 15 个问题修复、1 个问题经开发者确认忽略，并通过人工界面和 Windows x64 便携包启动验收
- V0.3 已完成固定 256 像素五级地图块、自由拖动、指针锚定缩放、坐标定位、三类标记、128 项内存缓存和按 Region 精确失效
- V0.3 已完成滚轮连续视觉缩放、最近五级吸附、旧图块过渡、默认地表总览和带颜色图例的标记开关
- V0.3 已完成双线程中心优先调度、100ms 渐进局部快照、实际可见区优先、完整后延迟预取、200ms 视口防抖、旧请求隔离、界面内当前世界缓存清理和当前视口 PNG 导出
- V0.3 已只读解析 `playerdata/*.dat` 和两种名称缓存格式，支持多人标记、带人数的玩家列表以及地表总览、`2 方块/像素` 的跨维度玩家定位
- 2026-07-29 的 V0.3 真实目录验收通过 41 个套件、152 项测试，主世界、下界、末地和 Mod 维度实际图块均生成成功，并从真实多人存档“刀剑”识别 4 名玩家和两个维度；相关 Region、`level.dat` 与玩家数据文件大小、修改时间和 SHA-256 前后不变
- V0.3 已完成所有涉及生产文件的 Rule 16 审查；发现并修复 5 个高等级和 3 个中等级问题，当前活动问题为 0
- V0.3 已于 2026-07-29 通过开发者人工界面验收，源码版本定为 `0.3.0`；不构建软件包、不创建标签或 Release
- V0.3.1 已完成失败图块局部恢复、玩家 Tooltip 与短时高亮、缓存统计和保留视图的重新加载
- V0.3.1 已增加七个显示级别，最高支持一个方块显示为 `4×4` 屏幕像素，高倍率继续复用 `BLOCKS_1` 瓦片缓存
- 2026-07-30 的 V0.3.1 候选构建执行 44 个套件、172 项测试，0 失败、0 错误、0 跳过
- V0.3.1 本地候选 ZIP 已通过校验、纯净启动和携带 3835 个历史缓存/配置文件的升级启动验证

V0.3.1 已完成本地开发、验收、最终整体审查和正式发布。二维地图模式已形成稳定基线并将在后续版本长期保留。下一开发阶段为 V0.4 三维技术可行性验证，目前尚未开始。
