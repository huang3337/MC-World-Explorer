# 代码审查：PreviewCache.java

- **审查日期**：2026-07-18
- **审查工具**：Codex
- **审查范围**：`preview/PreviewCache` 的便携缓存写入、失效判断和路径命名
- **问题总数**：0 个（🔴 0 / 🟠 0 / 🟡 0 / 🟢 0）

## 审查结论

逐行验算后未发现需要单独登记的问题，当前实现符合 V0.2 路线图和既有重大决策。

## 六维验算

- **错误处理**：空参数、源文件状态和缓存元数据不一致均有明确处理。
- **资源管理**：临时文件在 `finally` 清理，缓存替换优先采用原子移动。
- **安全性**：缓存目录名清理非法字符并附加世界绝对路径哈希，未发现目录逃逸。
- **代码质量**：缓存身份覆盖渲染版本、中心、`level.dat` 和 Region 状态。
- **模块耦合**：只依赖便携路径、世界信息和预览结果，不依赖 JavaFX。
- **UI 健壮性**：缓存损坏或失效返回未命中，由控制器重新生成。

## 验证证据

- 对应单元测试和真实目录集成测试纳入 V0.2 完整 Gradle 测试。
- 生产写入点和 Git 忽略边界在 V0.2 整体验收中统一复核。
