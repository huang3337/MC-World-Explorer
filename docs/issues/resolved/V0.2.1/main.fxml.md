# 代码审查：main.fxml

- **审查日期**：2026-07-24
- **审查工具**：Codex
- **审查范围**：世界预览中的维度选择、地表总览按钮、平滑高度滑块、状态、进度和导出控件绑定
- **问题总数**：0 个（🔴 0 / 🟠 0 / 🟡 0 / 🟢 0）

## 当前结论

当前界面使用维度下拉框、地表总览按钮、当前高度标签和平滑滑块。静态核对 25 个 `fx:id` 和 3 个 `onAction`，全部能对应当前控制器成员，未发现需要登记的 FXML 问题。

## 审查结论

新增控件位于既有预览页，不改变窗口结构；当前界面由维度下拉框、地表总览按钮、当前 Y/高度带标签和平滑高度滑块组成，FXML ID 与控制器字段一致，初始禁用状态正确。

## 六维验算

- **错误处理**：初始状态不触发生成，失败文本由控制器显示。
- **资源管理**：控件由 JavaFX 管理。
- **安全性**：FXML 不包含脚本或外部资源路径。
- **代码质量**：只增加本版本需要的维度选择、地表总览切换和高度滑块控件，没有嵌套工具卡片或无关布局重构。
- **模块耦合**：事件逻辑保留在控制器。
- **UI 健壮性**：高度标签保留稳定宽度，滑块随可用空间伸缩，生成状态与预览区域保持既有伸缩约束。

## 验证证据

- 2026-07-18 Gradle `run` 实际启动后 FXML 成功加载，标准错误为空。
- 2026-07-24 当前绑定静态复核通过；27 个测试套件、115 个测试在真实目录环境全部通过。

本记录已经与当前滑块界面重新对齐；控制器同日发现的两个问题已修复并归档。JavaFX 图片解码失败后的实际点击路径仍需在最终人工界面验收中关注。
