# 代码审查：SurfaceSampler.java

- **审查日期**：2026-07-24
- **审查工具**：Codex
- **审查范围**：地表总览、32 格高度带和区块 Section 范围采样
- **问题总数**：0 个（🔴 0 / 🟠 0 / 🟡 0 / 🟢 0）

## 当前结论

当前采样器兼容现代和旧 Palette，正确处理跨 Section 两格空间、缺失 Section、负高度和 32 格范围；合成与真实目录测试均通过，未发现需要登记的问题。

## 审查结论

新增高度带路径复用既有受限 NBT 解析和 Palette 存储，不改变地表总览结果；跨 Section 的两格空间检查和缺失 Section 空气语义均有测试。

## 六维验算

- **错误处理**：损坏 NBT、Section、Palette 和位存储继续映射结构化原因。
- **资源管理**：区块输入流使用 try-with-resources 关闭。
- **安全性**：NBT 读取上限保持 64 MiB，数组索引和 Palette 索引均验证。
- **代码质量**：解析结果集中为 `ParsedChunk`，两类采样共享一次解析。
- **模块耦合**：只依赖 Region 区块值和预览值对象，不依赖 UI/缓存。
- **UI 健壮性**：空图层返回空列，由渲染器稳定显示背景。

## 验证证据

- `SurfaceSamplerTest` 覆盖地表回归、同 Section 和跨 Section 高度带。
- 下界高度带和真实 Mod 维度集成生成通过。
