# 代码审查：measure-v04.ps1

- **审查日期**：2026-08-01
- **审查工具**：Codex
- **审查范围**：真实存档哈希、两后端性能与工作集测量
- **问题总数**：1 个（严重 0 / 高 0 / 中 1 / 低 0）

### ISSUE-V04MEASURE-001：冷测请求截图但脚本只验证 JSON 报告

- **严重程度**：中
- **类别**：错误处理
- **文件**：`scripts/v04/measure-v04.ps1`
- **行号**：审查时 Start-MeasuredTrial 报告校验段
- **状态**：已修复

**问题描述**：
截图写入失败时后端可能仍以 0 退出，脚本只检查 report 是否存在。

**当前代码（审查时快照）**：

```powershell
if (-not (Test-Path -LiteralPath $report -PathType Leaf)) {
    throw "... did not produce report"
}
```

**问题分析**：
最终证据可能声明完成图形冒烟，但实际缺少 PNG。

**建议修改**：
当 Screenshot 开关存在时同时要求 PNG 普通文件存在。

**影响范围**：
同条件截图和自动验收完整性。

- **解决日期**：2026-08-01
- **实际修改**：新增截图文件存在性阻断检查。
- **验证证据**：正式测量生成 6 张长测截图；两个便携副本截图也均存在并可读取。
