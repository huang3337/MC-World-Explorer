# README 视觉与信息结构改版设计

- **设计日期**：2026-07-30
- **确认状态**：开发者已确认
- **修改范围**：仅修改 `README.md` 的信息结构、排版和过时描述，不修改程序代码、开发路线或发布状态

## 目标

- 让首次访问仓库的用户立即看清当前可下载的软件包版本。
- 将软件包发布路线与源码开发进度明确分开，避免把已完成源码误解为已发布软件包。
- 减少重复功能清单，按用途组织内容，提高扫描和查找效率。
- 使用 GitHub 原生 Markdown 与 HTML 能力，保证浅色和深色主题下都具有可靠对比度。

## 顶部区域

保留现有项目图标、居中标题、项目副标题、Shields.io 徽章和主要链接。

顶部主要链接保留：

- 下载最新软件包
- 查看项目路线图
- 从源码运行

不加入自定义 CSS、脚本或固定正文颜色。标题和正文完全使用 GitHub 原生主题颜色。

## 软件包状态

在项目介绍之前增加醒目的软件包状态表：

- 当前最新软件包：V0.2.1
- 支持平台：Windows x64
- 运行环境：软件包内置 Java 21
- 下一个计划发布：V0.3.1

V0.3 不出现在已发布软件包列表中。

## 源码开发状态

软件包状态下方单独增加源码开发状态表：

- 最新完成源码：V0.3，已完成开发和人工验收
- 下一开发阶段：V0.3.1，尚未开始

使用 GitHub 原生提示块说明：

- V0.3 只完成并同步源码，没有构建软件包。
- V0.3.1 完成最后完善后，再评估标签、软件包和 GitHub Release。

## 软件包发布路线

发布路线只描述软件包，不混入纯源码版本：

1. V0.1.1：已发布，存档扫描和基础信息浏览。
2. V0.2.1：当前最新软件包，多维度预览与洞穴高度带。
3. V0.3.1：计划发布，整合 V0.3 交互地图与最后完善。

路线使用 Markdown 表格呈现，状态分别标注为“已发布”“当前版本”“计划发布”。

## 内容分组

将原有按 V0.1.1、V0.2.1 分开的长功能列表合并为“核心能力”，使用三列 Markdown 表格：

- 存档浏览
- 交互地图
- 定位与标记

快速开始保留四步流程：

1. 下载 V0.2.1。
2. 完整解压 ZIP。
3. 启动 `MC World Explorer.exe`。
4. 选择 Minecraft 目录或世界目录。

保留软件包完整解压、目录结构和 SmartScreen 提示。

## 存档安全

修正当前 README 中“不读取 `playerdata/*.dat`”的过时描述。

准确表述为：

- 只读访问 `level.dat`、`playerdata/*.dat`、`icon.png`、目录元数据和 Region 文件。
- 不修改、移动或删除 Minecraft 存档。
- 日志、缓存、导出和配置保存在程序根目录。

## 文档与源码运行

保留源码运行命令、运行数据目录、路线图、各版本进度记录、下载链接和许可证。

开发状态段落不再重复完整功能列表，只提供源码版本状态和相关文档入口。

## 明确移除

README 不展示以下会频繁变化的开发验证统计：

- 自动化测试数量
- 生产文件审查数量
- 活动 Issue 数量
- 存档 SHA-256 验证统计

这些信息继续以 `docs/progress/V0.3.md` 和逐文件审查记录为准。

## 验收标准

- 首屏可以明确区分“当前可下载软件包 V0.2.1”和“已完成源码 V0.3”。
- 软件包发布路线只包含 V0.1.1、V0.2.1 和计划发布的 V0.3.1。
- V0.3 被明确标记为没有软件包的源码版本。
- 页面不使用会破坏 GitHub 明暗主题对比度的固定文字颜色。
- 功能、快速开始、安全边界、源码运行和文档链接可以快速定位。
- README 中不再出现“不读取 `playerdata/*.dat`”的错误描述。
