# DECISION-001：采用严格便携的本地数据存储模式

- **状态**：已确认
- **确认日期**：2026-07-14
- **确认人**：项目开发者
- **适用版本**：V0.2 及后续版本

## 决策背景

V0.1.1 将运行日志写入 `%APPDATA%\MC World Explorer\logs`。这与 Windows 便携版“解压即可使用、删除程序目录即可完整移除”的预期不一致，也会让用户难以判断程序在系统中留下了哪些文件。

V0.2 将新增世界缩略图缓存和用户导出文件，因此必须在开发前统一应用生成数据的保存位置。

## 备选方案

### 方案一：使用 AppData

将缓存、日志和配置保存在 `%LOCALAPPDATA%` 或 `%APPDATA%`。

优点是通常具有写入权限，符合常规安装版 Windows 应用习惯；缺点是删除便携版程序目录后仍会留下数据，不符合本项目的便携目标。

### 方案二：程序目录优先，失败时静默回退 AppData

优点是兼顾部分便携性和写入成功率；缺点是实际数据位置不确定，用户无法通过删除程序目录完成干净移除。

### 方案三：严格便携模式

所有由应用生成的数据都保存在程序根目录，不使用 AppData。程序目录不可写时向用户显示明确错误，不静默回退到系统目录。

## 最终选择

采用方案三：严格便携模式。

```text
MC World Explorer/
├─ MC World Explorer.exe
├─ app/
├─ runtime/
├─ cache/
│  └─ 世界标识/
│     ├─ world_preview.png
│     └─ preview-metadata.json
├─ logs/
│  └─ mc-world-explorer.log
├─ exports/
│  └─ 世界名称-导出时间.png
└─ README.txt
```

开发环境运行时，项目根目录视为程序根目录；打包后运行时，EXE 所在目录视为程序根目录。

## 选择原因

- 用户可以通过删除整个程序目录完整移除应用及其生成数据。
- 缓存、日志和导出文件的位置清晰、可见、可备份。
- 不向 Minecraft 存档目录写入任何数据，继续保持存档只读边界。
- 避免程序目录和 AppData 同时存在数据，降低排查与清理成本。
- 与 Windows x64 便携版的产品定位一致。

## 行为约束

- 缩略图内部缓存写入程序根目录的 `cache/`。
- 运行日志写入程序根目录的 `logs/`。
- 用户主动导出的图片默认写入程序根目录的 `exports/`。
- 后续如增加配置文件，应写入程序根目录的 `config/`。
- 不使用 `%APPDATA%` 或 `%LOCALAPPDATA%` 保存应用生成数据。
- 不向 Minecraft 世界目录写入缓存、日志、预览图或配置。
- 程序目录不可写时必须显示明确提示，不允许静默回退 AppData。
- `cache/`、`logs/`、`exports/` 和未来的 `config/` 不纳入 Git。
- 用户导出时如果程序目录不可写，可以由用户明确选择其他目录；该行为不属于静默回退。

## 对 V0.2 的影响

- V0.2 实现缩略图前，必须先修改 Logback 配置，使日志写入本地 `logs/`。（已完成）
- 必须统一开发环境与 jpackage 便携版的程序根目录解析方式。（已完成）
- 缩略图缓存失效信息与 PNG 一起保存在本地 `cache/`。
- 导出逻辑默认创建本地 `exports/`，并处理目录不可写和文件名冲突。
- 相关路径解析、不可写目录和回退提示必须有测试覆盖。
- README、软件包内 README 和 V0.2 进度记录必须同步说明严格便携模式。

## V0.2 落实状态

2026-07-14 完成第一阶段实现：

- 新增 `PortablePaths`，通过主 JAR、开发目录或便携运行时布局识别程序根目录。
- `Launcher` 在 Logback 和 JavaFX 应用初始化前验证程序根目录下的 `logs/` 可写。
- 程序目录不可写时显示明确错误，不回退 AppData。
- Logback 改为写入 `${mcworldexplorer.home}/logs`。
- 开发环境实际启动时日志根为项目目录。
- 临时 jpackage 应用镜像从无关工作目录启动时，日志根仍为 EXE 所在目录。
- 两类启动验收均未修改或重新创建 `%APPDATA%\MC World Explorer`。

## 本机旧日志迁移记录

2026-07-14 对本机 `%APPDATA%\MC World Explorer\logs` 执行一次人工迁移：先复制到项目根目录 `logs/migrated-appdata-20260714/`，逐个校验 SHA-256 一致后删除原文件和空目录。

| 文件 | 字节数 | SHA-256 |
|---|---:|---|
| `mc-world-explorer.2026-07-11.0.log.gz` | 8,784 | `38a369ff813c00b841b2d8429b76896a6a64ccf0341ae99a12275b817deea150` |
| `mc-world-explorer.2026-07-12.0.log.gz` | 2,893 | `fba0370ab2171a328e5f8ad12c12b9042e41a61ec86d296c8b5f399aaf868df3` |
| `mc-world-explorer.log` | 205,830 | `a15cdf8992a98afb5fc9e8bec9ed8cba6494945b457cfcdb5dd2aa1f49a4e77c` |

迁移完成后，原 `%APPDATA%\MC World Explorer` 目录已不存在，迁移目标受现有 `logs/` Git 忽略规则保护。

V0.2 开发前运行旧日志配置的基线测试时，AppData 又生成了一个日志文件。完成新实现后，将其迁移到 `logs/migrated-appdata-20260714-v0.2-baseline/`，校验一致后删除源目录。

| 文件 | 字节数 | SHA-256 |
|---|---:|---|
| `mc-world-explorer.log` | 20,430 | `09df6d8b69c8a3ee674cbaa4dc5773751544c2c1f4da51f5d53e82457786de0d` |

迁移后重新执行完整测试，`%APPDATA%\MC World Explorer` 没有重新生成。

## 兼容与迁移说明

本次只迁移当前开发机器上的既有日志，不在应用中实现自动删除 AppData 的逻辑，避免程序擅自删除用户文件。

已经发布的 V0.1.1 软件包仍使用旧日志路径，运行它仍可能重新创建 AppData 日志目录。当前 `main` 的 V0.2 开发代码已经完成日志路径修改，并通过代码、测试、开发环境和临时 jpackage 应用镜像验证不再向 AppData 写入数据。

## 长期影响

严格便携模式要求程序安装目录具有写入权限。用户如果将程序放入 `Program Files` 等受保护目录，应用应说明便携版需要移动到普通可写目录，而不是悄悄改变数据位置。

未来若增加安装版，需要另行提出重大决策，不能直接改变便携版的存储约定。
